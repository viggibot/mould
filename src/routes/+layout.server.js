// Prerender everything to static HTML — fastest to load and easiest for
// search engines to crawl. Remove this only if you add server-only routes.
export const prerender = true;
export const trailingSlash = 'never';